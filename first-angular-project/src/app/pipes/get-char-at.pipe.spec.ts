import { GetCharAtPipe } from './get-char-at.pipe';

describe('GetCharAtPipe', () => {
  it('create an instance', () => {
    const pipe = new GetCharAtPipe();
    expect(pipe).toBeTruthy();
  });
});
